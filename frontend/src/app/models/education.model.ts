export interface Education {
  institution: string;
  degree: string;
  major: string;
  graduationYear: string;
  location: string;
  description?: string; // Optional: for additional details about the education
}
